-- Add sample data for testing
-- This is an example migration script

-- Insert sample user data (optional)
-- INSERT INTO users (username, password, email, name, created_at, role) 
-- VALUES ('testuser', '$2a$10$example', 'test@example.com', 'Test User', NOW(), 'USER');

-- Add any additional columns or modifications here as examples
-- ALTER TABLE portfolio ADD COLUMN IF NOT EXISTS created_at TIMESTAMP DEFAULT NOW();
-- ALTER TABLE portfolio ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP DEFAULT NOW();

-- This file serves as an example for future migrations
-- Remove or modify as needed for your specific requirements